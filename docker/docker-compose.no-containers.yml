services:
  main:
    image: busybox:1.36
    stop_grace_period: 1s
    command: >
      sh -c "
        echo 'main started (no healthcheck)';
        exec tail -f /dev/null
      "

  ghost:
    image: busybox:1.36
    stop_grace_period: 1s
    command: ["sh", "-c", "exec tail -f /dev/null"]
