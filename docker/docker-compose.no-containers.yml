services:
  main:
    image: nginx:1.29-alpine
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost"]
      interval: 1s
      timeout: 2s
      retries: 30
      start_period: 0s

  ghost:
    image: alpine:3.23
    command: ["sh", "-c", "sleep 300"]
