services:
  oneshot-ok:
    image: alpine:3.20
    command: ["sh", "-c", "echo 'one-shot ok' && exit 0"]
    restart: "no"

  oneshot-fail:
    image: alpine:3.20
    command: ["sh", "-c", "echo 'one-shot fail' >&2 && exit 42"]
    restart: "no"
