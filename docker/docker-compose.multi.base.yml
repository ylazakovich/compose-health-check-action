services:
  api:
    image: busybox:1.36
    stop_grace_period: 1s
    command: >
      sh -c "
        echo 'API starting (multi base)...';
        touch /tmp/ready;
        exec tail -f /dev/null
      "
